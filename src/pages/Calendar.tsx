import React from 'react';

export default function Calendar() {
  return (
    <div className="max-w-5xl mx-auto p-3">
      <div className="text-lg font-semibold mb-3">Calendar (MVP stub)</div>
      <div className="text-sm text-gray-600">
        In MVP, tasks are visible by due dates on the Tasks list. Drag/drop date edits and full calendar will come in phase-2.
      </div>
    </div>
  );
}
